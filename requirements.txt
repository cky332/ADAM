# Core (required) -- the full simulation runs with only these.
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7

# Optional -- enable the *real* backends so the identical code reproduces the
# paper's exact numbers instead of the calibrated simulation.
#   sentence-transformers>=2.2   # real all-MiniLM-L6-v2 / e5 / gte encoders (Fig 3g)
#   openai>=1.0                  # real attacker/victim LLM via OPENAI_API_KEY
#   torch                        # backs sentence-transformers
